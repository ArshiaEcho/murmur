# Screenshots (to capture before handing to Claude Design)

Drop PNGs of the running Murmur app here, light AND dark mode. Suggested filenames:

- `sessions-dashboard-light.png` / `sessions-dashboard-dark.png` — the project-tile grid.
- `sessions-drawer-light.png` / `sessions-drawer-dark.png` — a session open in the right-side
  chat drawer (transcript + summary + Ask box + mic).
- `sessions-swatch.png` — a project's color picker open.
- `overview.png` — the Overview/home tab.
- `onboarding-welcome.png`, `onboarding-accessibility.png`, `onboarding-model.png`,
  `onboarding-finish.png` — the four first-run screens.

Capture a window with macOS: `Cmd-Shift-4` then Space then click the Murmur window (or
`screencapture -w out.png`). The full list/rationale is in `../redesign-brief.md`.
